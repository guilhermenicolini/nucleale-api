export enum AccountStatus {
  awaitingVerification = 'awaitingVerification',
  active = 'active'
}

export enum AccountRoles {
  user = 'user',
  admin = 'admin'
}

export enum Gender {
  male = 'male',
  female = 'female'
}
